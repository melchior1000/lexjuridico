(function(){
'use strict';
const $=(s,r=document)=>r.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const procs=()=>{try{return typeof getProcs==='function'?(getProcs()||[]):[]}catch{return[]}};
const days=p=>{const raw=p?.prazoReal||p?.prazo||p?.dataPrazo||p?.next_action_due_at;if(!raw)return 9999;let d;if(/^\d{2}\/\d{2}\/\d{4}$/.test(raw)){const[a,b,c]=raw.split('/');d=new Date(+c,+b-1,+a)}else d=new Date(raw);if(Number.isNaN(d.getTime()))return 9999;const n=new Date();return Math.ceil((d-n)/86400000)};
const dock=()=>'<nav class="lex-dock"><button class="on" onclick="lexHome()"><b>⌂</b><span>Início</span></button><button onclick="lexProcessos()"><b>▣</b><span>Processos</span></button><button class="lex-main" onclick="lexChat()"><b>◉</b><span>LEX</span></button><button onclick="lexPrazos()"><b>◷</b><span>Prazos</span></button><button onclick="lexMais()"><b>☰</b><span>Mais</span></button></nav>';
const card=(tone,title,meta,detail,action,label)=>'<article class="lex-today-item '+tone+'"><div><small>'+esc(meta)+'</small><h2>'+esc(title)+'</h2><p>'+esc(detail)+'</p></div><button onclick="'+action+'">'+esc(label)+'</button></article>';
async function api(path){const r=await fetch(path);if(!r.ok)throw new Error(path);return r.json()}
async function today(){const ps=procs(),items=[];for(const p of ps){const d=days(p);if(d<=0)items.push({rank:0,html:card('critical',p.nome||p.numero||'Prazo crítico',(p.case_type||'judicial')+' · '+(p.numero||'sem número'),d<0?'Prazo cadastrado vencido. Confirme a situação jurídica antes de agir.':'Prazo cadastrado para hoje.','lexOpenProc(\''+esc(String(p.id))+'\')','Resolver')})}
 let tasks=[];try{const d=await api('/api/trabalho');tasks=d.tarefas||[]}catch{}for(const t of tasks.filter(x=>['bloqueada','aguardando_documento','aguardando_revisao'].includes(String(x.status))).slice(0,4)){items.push({rank:1,html:card('blocked',t.titulo||t.tipo||'Tarefa precisa de você','Tarefa · '+(t.status||''),t.motivo||t.pendencia||'Há uma pendência que impede a continuação.','lexChat()','LEX, resolva')})}
 try{const d=await api('/api/escritorio/recepcao');for(const r of (d.itens||d.contatos||d.recepcao||[]).filter(x=>x.status!=='arquivado').slice(0,3)){items.push({rank:2,html:card('waiting',r.nome||r.cliente||'Cliente aguardando',r.canal||'Recepção',r.assunto||r.ultima_mensagem||'Aguardando atendimento.','typeof lexOpenReception===\'function\'?lexOpenReception():lexChat()','Ver resposta')})}}catch{}
 for(const p of ps){for(const d of [...(p.entrada_processual||[]),...(p.arquivos||[]),...(p.recebimentos||[])]){if(/quar|confer|reject/i.test(String(d.status||d.motivo||'')))items.push({rank:1,html:card('quarantine',d.nome||d.arquivo||'Documento em quarentena','Documento · '+(p.nome||p.numero||''),d.motivo||'Precisa de conferência antes de entrar no fluxo produtivo.','lexOpenProc(\''+esc(String(p.id))+'\')','Ver problema')})}}
 items.sort((a,b)=>a.rank-b.rank);const body='<main class="lex-screen lex-today"><header class="lex-top"><div><strong>LEX</strong><small>HOJE</small></div><div class="lex-top-actions"><button onclick="lexToggleTheme()" aria-label="Tema">◐</button><button onclick="typeof toggleSidebar===\'function\'&&toggleSidebar()">☰</button></div></header><section class="lex-today-head"><small>ESCRITÓRIO</small><h1>'+(items.length?'Há '+items.length+' '+(items.length===1?'coisa que precisa':'coisas que precisam')+' de você.':'Tudo sob controle.')+'</h1><p>'+(items.length?'Só mostramos decisões e exceções reais.':'Nenhuma exceção crítica encontrada agora.')+'</p></section><section class="lex-today-list">'+(items.length?items.slice(0,8).map(x=>x.html).join(''):'<div class="lex-today-clear"><strong>Tudo sob controle.</strong><span>O LEX continua trabalhando em segundo plano.</span></div>')+'</section>'+dock()+'</main>';const host=$('#content');if(host){document.body.classList.add('lex-commercial','lex2-core');host.innerHTML=body}}
 function patch(){window.lexHome=today;const legacy=window.home;if(legacy&&legacy!==today)window.lexLegacyHome=legacy}
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(patch,0),{once:true});else setTimeout(patch,0);
})();
